<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Store - <%= settings.name %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet"/>
    <style>
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .animate-fade-in {
            animation: fadeIn 0.3s ease-out forwards;
        }
    </style>
</head>
<body class="bg-[#0F0F0F] text-white min-h-screen">
    <div class="flex">
        <!-- Sidebar -->
        <%- include('../components/navigation') %>
        
        <!-- Main Content -->
        <main class="flex-1 ml-72 p-8">
            <!-- Header -->
            <div class="flex items-center justify-between mb-8">
                <div>
                    <h1 class="text-3xl font-bold">Store</h1>
                    <p class="text-gray-400">Use your <%= settings.api.client.coins.name %> to upgrade your servers</p>
                </div>
                <div class="flex items-center space-x-3 p-3 rounded-xl bg-[#1A1A1A]">
                    <i class="fas fa-coins text-yellow-500"></i>
                    <span class="font-medium"><%= coins %> <%= settings.api.client.coins.name %></span>
                </div>
            </div>

            <!-- Error Messages -->
            <% if (req.query.err) { %>
                <% if (req.query.err !== "none") { %>
                    <% if (req.query.err == "CANNOTAFFORD") { %>
                        <div class="bg-red-500/10 border border-red-500/20 rounded-xl p-4 mb-6 flex items-center justify-between animate-fade-in">
                            <span class="text-red-400">Insufficient funds.</span>
                            <button class="text-red-400 hover:text-red-300">
                                <i class="fas fa-times"></i>
                            </button>
                        </div>
                    <% } else if (req.query.err == "MAXCPUEXCEETED") { %>
                        <div class="bg-red-500/10 border border-red-500/20 rounded-xl p-4 mb-6 flex items-center justify-between animate-fade-in">
                            <span class="text-red-400">You have reached the CPU limit!</span>
                            <button class="text-red-400 hover:text-red-300">
                                <i class="fas fa-times"></i>
                            </button>
                        </div>
                    <% } else if (req.query.err == "MAXRAMEXCEETED") { %>
                        <div class="bg-red-500/10 border border-red-500/20 rounded-xl p-4 mb-6 flex items-center justify-between animate-fade-in">
                            <span class="text-red-400">You have reached the RAM limit!</span>
                            <button class="text-red-400 hover:text-red-300">
                                <i class="fas fa-times"></i>
                            </button>
                        </div>
                    <% } else if (req.query.err == "MAXDISKEXCEETED") { %>
                        <div class="bg-red-500/10 border border-red-500/20 rounded-xl p-4 mb-6 flex items-center justify-between animate-fade-in">
                            <span class="text-red-400">You have reached the disk limit!</span>
                            <button class="text-red-400 hover:text-red-300">
                                <i class="fas fa-times"></i>
                            </button>
                        </div>
                    <% } else if (req.query.err == "MAXSERVERSEXCEETED") { %>
                        <div class="bg-red-500/10 border border-red-500/20 rounded-xl p-4 mb-6 flex items-center justify-between animate-fade-in">
                            <span class="text-red-400">You have reached the servers limit!</span>
                            <button class="text-red-400 hover:text-red-300">
                                <i class="fas fa-times"></i>
                            </button>
                        </div>
                    <% } %>
                <% } else if (req.query.err == "none") { %>
                    <div class="bg-green-500/10 border border-green-500/20 rounded-xl p-4 mb-6 flex items-center justify-between animate-fade-in">
                        <span class="text-green-400">Success!</span>
                        <button class="text-green-400 hover:text-green-300">
                            <i class="fas fa-times"></i>
                        </button>
                    </div>
                <% } %>
            <% } %>

            <!-- Store Items Grid -->
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
                <!-- RAM Card -->
                <div class="bg-[#1A1A1A] rounded-xl p-6 hover:bg-[#242424] transition-all duration-300 animate-fade-in">
                    <div class="w-12 h-12 rounded-xl bg-purple-500/10 flex items-center justify-center mb-4">
                        <i class="fas fa-memory text-purple-500 text-xl"></i>
                    </div>
                    <h3 class="text-xl font-semibold mb-2">RAM</h3>
                    <p class="text-gray-400 mb-4">Upgrade your server's memory</p>
                    <div class="flex items-center justify-between mb-6">
                        <span class="text-lg font-medium"><%= settings.api.client.coins.store.ram.cost %> <%= settings.api.client.coins.name %> / GB</span>
                    </div>
                    <a href="/buy?type=ram&amount=1" class="block w-full py-3 rounded-xl bg-gradient-to-r from-purple-500 to-purple-600 hover:from-purple-600 hover:to-purple-700 text-white font-semibold text-center transition-all duration-300 shadow-lg hover:shadow-purple-500/20">
                        Buy RAM
                    </a>
                </div>

                <!-- Disk Card -->
                <div class="bg-[#1A1A1A] rounded-xl p-6 hover:bg-[#242424] transition-all duration-300 animate-fade-in">
                    <div class="w-12 h-12 rounded-xl bg-yellow-500/10 flex items-center justify-center mb-4">
                        <i class="fas fa-hdd text-yellow-500 text-xl"></i>
                    </div>
                    <h3 class="text-xl font-semibold mb-2">Disk</h3>
                    <p class="text-gray-400 mb-4">Expand your storage capacity</p>
                    <div class="flex items-center justify-between mb-6">
                        <span class="text-lg font-medium"><%= settings.api.client.coins.store.disk.cost %> <%= settings.api.client.coins.name %> / 5GB</span>
                    </div>
                    <a href="/buy?type=disk&amount=5" class="block w-full py-3 rounded-xl bg-gradient-to-r from-yellow-500 to-yellow-600 hover:from-yellow-600 hover:to-yellow-700 text-white font-semibold text-center transition-all duration-300 shadow-lg hover:shadow-yellow-500/20">
                        Buy Disk
                    </a>
                </div>

                <!-- CPU Card -->
                <div class="bg-[#1A1A1A] rounded-xl p-6 hover:bg-[#242424] transition-all duration-300 animate-fade-in">
                    <div class="w-12 h-12 rounded-xl bg-green-500/10 flex items-center justify-center mb-4">
                        <i class="fas fa-microchip text-green-500 text-xl"></i>
                    </div>
                    <h3 class="text-xl font-semibold mb-2">CPU</h3>
                    <p class="text-gray-400 mb-4">Boost your processing power</p>
                    <div class="flex items-center justify-between mb-6">
                        <span class="text-lg font-medium"><%= settings.api.client.coins.store.cpu.cost %> <%= settings.api.client.coins.name %> / Core</span>
                    </div>
                    <a href="/buy?type=cpu&amount=1" class="block w-full py-3 rounded-xl bg-gradient-to-r from-green-500 to-green-600 hover:from-green-600 hover:to-green-700 text-white font-semibold text-center transition-all duration-300 shadow-lg hover:shadow-green-500/20">
                        Buy CPU
                    </a>
                </div>

                <!-- Slot Card -->
                <div class="bg-[#1A1A1A] rounded-xl p-6 hover:bg-[#242424] transition-all duration-300 animate-fade-in">
                    <div class="w-12 h-12 rounded-xl bg-blue-500/10 flex items-center justify-center mb-4">
                        <i class="fas fa-server text-blue-500 text-xl"></i>
                    </div>
                    <h3 class="text-xl font-semibold mb-2">Slots</h3>
                    <p class="text-gray-400 mb-4">Buy server slots</p>
                    <div class="flex items-center justify-between mb-6">
                        <span class="text-lg font-medium"><%= settings.api.client.coins.store.servers.cost %> <%= settings.api.client.coins.name %> / Slot</span>
                    </div>
                    <a href="/buy?type=servers&amount=1" class="block w-full py-3 rounded-xl bg-gradient-to-r from-blue-500 to-blue-600 hover:from-blue-600 hover:to-blue-700 text-white font-semibold text-center transition-all duration-300 shadow-lg hover:shadow-blue-500/20">
                        Buy Slots
                    </a>
                </div>
            </div>
        </main>
    </div>
</body>
</html>