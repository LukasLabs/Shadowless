<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile - <%= settings.name %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet"/>
    <style>
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .animate-fade-in {
            animation: fadeIn 0.3s ease-out forwards;
        }
    </style>
</head>
<body class="bg-[#0F0F0F] text-white min-h-screen">
    <div class="flex">
        <!-- Sidebar -->
        <%- include('../components/navigation') %>
        
        <!-- Main Content -->
        <main class="flex-1 ml-72 p-8">
            <!-- Header -->
            <div class="mb-8">
                <h1 class="text-3xl font-bold mb-2">Control Panel Credentials</h1>
                <p class="text-gray-400">On this page you can find credentials you can use to access your hosted services.</p>
            </div>

            <!-- Pterodactyl Panel Card -->
            <div class="bg-[#1A1A1A] rounded-xl p-6 mb-6 animate-fade-in">
                <div class="flex items-center space-x-3 mb-4">
                    <img src="https://deploy.creepercloud.io/images/logos/pterodactyl.svg" class="w-8 h-8" alt="Pterodactyl Logo">
                    <h2 class="text-xl font-bold">Manage your game servers</h2>
                </div>
                
                <p class="text-gray-400 mb-6">Use the following credentials to login to Pterodactyl™.</p>
                
                <div class="space-y-3 mb-6">
                    <div class="flex items-center space-x-2">
                        <span class="font-medium text-gray-300">Email:</span>
                        <code class="text-gray-400"><%= pterodactyl.email %></code>
                    </div>
                    <div class="flex items-center space-x-2">
                        <span class="font-medium text-gray-300">Password:</span>
                        <span class="text-gray-400">Forgot it or never generated it? Reset it!</span>
                    </div>
                </div>

                <div class="flex space-x-3">
                    <a href="/security" class="flex-1 flex items-center justify-center space-x-2 py-3 rounded-xl bg-gradient-to-r from-red-500 to-red-600 hover:from-red-600 hover:to-red-700 text-white font-semibold transition-all duration-300 shadow-lg hover:shadow-red-500/20">
                        <i class="fas fa-key"></i>
                        <span>Reset password</span>
                    </a>
                    <a href="<%= settings.pterodactyl.domain %>" target="_blank" rel="noreferrer" class="flex-1 flex items-center justify-center space-x-2 py-3 rounded-xl bg-gradient-to-r from-blue-500 to-blue-600 hover:from-blue-600 hover:to-blue-700 text-white font-semibold transition-all duration-300 shadow-lg hover:shadow-blue-500/20">
                        <i class="fas fa-external-link-alt"></i>
                        <span>Open game panel</span>
                    </a>
                </div>
            </div>

            <!-- Additional Services Section -->
            <div class="bg-[#1A1A1A] rounded-xl p-6 animate-fade-in">
                <h2 class="text-xl font-bold mb-4">Additional Services</h2>
                <p class="text-gray-400">More services will be added here in the future.</p>
            </div>
        </main>
    </div>
</body>
</html>