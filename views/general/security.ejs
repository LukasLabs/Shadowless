<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8"/>
    <meta content="width=device-width, initial-scale=1.0" name="viewport"/>
    <title><%= settings.name %> - Security</title>
    <link rel="icon" type="image/png" href="https://i.imgur.com/GY3cXet.png">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet"/>
    <style>
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .animate-fade-in {
            animation: fadeIn 0.3s ease-out forwards;
        }
        .shadow-glow {
            box-shadow: 0 0 15px rgba(99, 102, 241, 0.3);
        }
        .gradient-text {
            background: linear-gradient(45deg, #6366f1, #8b5cf6);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        .bg-grid {
            background-image: radial-gradient(rgba(99, 102, 241, 0.1) 1px, transparent 1px);
            background-size: 20px 20px;
        }
        .card-hover:hover {
            transform: translateY(-3px);
        }
        @keyframes pulse {
            0%, 100% { opacity: 0.8; }
            50% { opacity: 1; }
        }
        .animate-pulse-slow {
            animation: pulse 3s infinite ease-in-out;
        }
        .security-gradient {
            background: linear-gradient(135deg, rgba(239, 68, 68, 0.1), rgba(99, 102, 241, 0.1));
        }
    </style>
</head>
<body class="bg-black text-white min-h-screen">
    <div class="flex">
        <!-- Sidebar -->
        <%- include('../components/navigation') %>
        
        <!-- Main Content -->
        <main class="flex-1 ml-72 p-8">
            <!-- Header Section -->
            <div class="relative rounded-2xl overflow-hidden mb-8 animate-fade-in shadow-glow">
                <div class="absolute inset-0 bg-gradient-to-r from-indigo-600/20 to-purple-600/20 backdrop-blur-sm bg-grid"></div>
                <div class="relative p-8">
                    <h1 class="text-4xl font-bold mb-2 gradient-text">Account Security</h1>
                    <p class="text-gray-300">Protect your account with advanced security settings and authentication options.</p>
                </div>
            </div>

            <!-- Security Status Card -->
            <div class="mb-8 grid grid-cols-1 md:grid-cols-3 gap-6">
                <!-- Account Shield -->
                <div class="bg-gray-900/80 border border-indigo-500/20 rounded-xl p-6 hover:bg-gray-900/90 transition-all duration-300 animate-fade-in card-hover shadow-lg">
                    <div class="flex items-center mb-4">
                        <div class="w-12 h-12 rounded-xl bg-indigo-500/10 flex items-center justify-center mr-4">
                            <i class="fas fa-shield-alt text-indigo-500 text-xl"></i>
                        </div>
                        <div>
                            <h3 class="font-semibold text-lg text-white">Account Shield</h3>
                            <p class="text-gray-400 text-sm">Basic protection active</p>
                        </div>
                    </div>
                    <div class="w-full h-2 bg-gray-800 rounded-full overflow-hidden">
                        <div class="h-full w-2/3 bg-indigo-500 transition-all duration-500"></div>
                    </div>
                    <div class="mt-3 text-xs text-gray-400">
                        <span>Security Score: <span class="text-indigo-400">65%</span></span>
                    </div>
                </div>

                <!-- Last Login -->
                <div class="bg-gray-900/80 border border-indigo-500/20 rounded-xl p-6 hover:bg-gray-900/90 transition-all duration-300 animate-fade-in card-hover shadow-lg">
                    <div class="flex items-center mb-4">
                        <div class="w-12 h-12 rounded-xl bg-green-500/10 flex items-center justify-center mr-4">
                            <i class="fas fa-clock text-green-500 text-xl"></i>
                        </div>
                        <div>
                            <h3 class="font-semibold text-lg text-white">Last Login</h3>
                            <p class="text-gray-400 text-sm">Current Session</p>
                        </div>
                    </div>
                    <div class="flex items-center space-x-2 text-sm text-gray-300">
                        <i class="fas fa-calendar-alt text-green-400"></i>
                        <span>Today, <%= new Date().toLocaleDateString() %></span>
                    </div>
                    <div class="flex items-center space-x-2 mt-2 text-sm text-gray-300">
                        <i class="fas fa-map-marker-alt text-green-400"></i>
                        <span>Your Location</span>
                    </div>
                </div>

                <!-- Security Alerts -->
                <div class="bg-gray-900/80 border border-indigo-500/20 rounded-xl p-6 hover:bg-gray-900/90 transition-all duration-300 animate-fade-in card-hover shadow-lg">
                    <div class="flex items-center mb-4">
                        <div class="w-12 h-12 rounded-xl bg-red-500/10 flex items-center justify-center mr-4">
                            <i class="fas fa-bell text-red-500 text-xl"></i>
                        </div>
                        <div>
                            <h3 class="font-semibold text-lg text-white">Security Alerts</h3>
                            <p class="text-gray-400 text-sm">No active alerts</p>
                        </div>
                    </div>
                    <div class="flex items-center py-2 px-3 bg-gray-800/50 rounded-lg">
                        <div class="w-2 h-2 rounded-full bg-green-500 mr-3 animate-pulse-slow"></div>
                        <span class="text-sm text-gray-300">Your account is secure</span>
                    </div>
                </div>
            </div>

            <!-- Security Details -->
            <div class="bg-gray-900/80 border border-indigo-500/20 rounded-xl overflow-hidden mb-8 animate-fade-in shadow-lg">
                <div class="p-6 border-b border-indigo-500/10">
                    <h2 class="text-xl font-bold gradient-text">Security Information</h2>
                    <p class="text-gray-400 mt-1">Manage and update your account credentials</p>
                </div>

                <div class="space-y-6 p-6">
                    <!-- Username -->
                    <div class="flex flex-col md:flex-row md:items-center md:justify-between p-5 rounded-xl bg-gray-800/50 transition-all duration-300 hover:bg-gray-800/80 border border-indigo-500/10">
                        <div class="mb-3 md:mb-0">
                            <div class="text-sm text-gray-400 mb-1">Username</div>
                            <div class="font-medium text-white flex items-center space-x-2">
                                <div class="w-8 h-8 rounded-lg bg-blue-500/10 flex items-center justify-center">
                                    <i class="fas fa-user text-blue-500"></i>
                                </div>
                                <span><%= pterodactyl.username %></span>
                            </div>
                        </div>
                        <div class="flex items-center space-x-2">
                            <span class="px-3 py-1 rounded-lg bg-blue-500/10 text-blue-400 text-sm border border-blue-500/20">
                                <i class="fas fa-check-circle mr-1"></i> Verified
                            </span>
                        </div>
                    </div>

                    <!-- Email -->
                    <div class="flex flex-col md:flex-row md:items-center md:justify-between p-5 rounded-xl bg-gray-800/50 transition-all duration-300 hover:bg-gray-800/80 border border-indigo-500/10">
                        <div class="mb-3 md:mb-0">
                            <div class="text-sm text-gray-400 mb-1">Email Address</div>
                            <div class="font-medium text-white flex items-center space-x-2">
                                <div class="w-8 h-8 rounded-lg bg-purple-500/10 flex items-center justify-center">
                                    <i class="fas fa-envelope text-purple-500"></i>
                                </div>
                                <span><%= pterodactyl.email %></span>
                            </div>
                        </div>
                        <div class="flex items-center space-x-2">
                            <span class="px-3 py-1 rounded-lg bg-purple-500/10 text-purple-400 text-sm border border-purple-500/20">
                                <i class="fas fa-star mr-1"></i> Primary
                            </span>
                        </div>
                    </div>

                    <!-- Password -->
                    <div class="flex flex-col md:flex-row md:items-center md:justify-between p-5 rounded-xl bg-gray-800/50 transition-all duration-300 hover:bg-gray-800/80 border border-indigo-500/10">
                        <div class="mb-3 md:mb-0">
                            <div class="text-sm text-gray-400 mb-1">Password</div>
                            <div class="font-medium text-white flex items-center space-x-2">
                                <div class="w-8 h-8 rounded-lg bg-yellow-500/10 flex items-center justify-center">
                                    <i class="fas fa-key text-yellow-500"></i>
                                </div>
                                <span class="font-mono"><% if (req.session.password) { %><%= req.session.password %><% } else { %>••••••••<% } %></span>
                            </div>
                        </div>
                        <div>
                            <% if (req.session.password) { %>
                            <a href="../security" class="px-4 py-2 rounded-xl bg-gradient-to-r from-red-500 to-red-600 hover:from-red-600 hover:to-red-700 text-white font-medium text-sm transition-all duration-300 shadow-lg hover:shadow-red-500/20">
                                <i class="fas fa-eye-slash mr-2"></i> Hide Password
                            </a>
                            <% } else { %>
                            <a href="../regen" class="px-4 py-2 rounded-xl bg-gradient-to-r from-indigo-500 to-indigo-600 hover:from-indigo-600 hover:to-indigo-700 text-white font-medium text-sm transition-all duration-300 shadow-lg hover:shadow-indigo-500/20">
                                <i class="fas fa-eye mr-2"></i> Show Password
                            </a>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Security Tips -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
                <!-- Security Notice -->
                <div class="bg-gray-900/80 border border-yellow-500/20 rounded-xl p-5 hover:bg-gray-900/90 transition-all duration-300 animate-fade-in card-hover shadow-lg">
                    <div class="flex items-start space-x-4">
                        <div class="w-10 h-10 rounded-xl bg-yellow-500/10 flex items-center justify-center mt-1 flex-shrink-0">
                            <i class="fas fa-exclamation-triangle text-yellow-500"></i>
                        </div>
                        <div>
                            <h3 class="font-semibold text-yellow-400 mb-2">Security Notice</h3>
                            <p class="text-gray-300 text-sm">Keep your password secure and never share it with anyone. Change it immediately if you suspect unauthorized access to your account.</p>
                        </div>
                    </div>
                </div>

                <!-- Password Tips -->
                <div class="bg-gray-900/80 border border-blue-500/20 rounded-xl p-5 hover:bg-gray-900/90 transition-all duration-300 animate-fade-in card-hover shadow-lg overflow-hidden relative">
                    <div class="absolute top-0 right-0 w-24 h-24 opacity-10">
                        <i class="fas fa-fingerprint text-6xl text-blue-500 absolute top-4 right-4"></i>
                    </div>
                    <div class="flex items-start space-x-4">
                        <div class="w-10 h-10 rounded-xl bg-blue-500/10 flex items-center justify-center mt-1 flex-shrink-0">
                            <i class="fas fa-lightbulb text-blue-500"></i>
                        </div>
                        <div>
                            <h3 class="font-semibold text-blue-400 mb-2">Password Tips</h3>
                            <p class="text-gray-300 text-sm">Use a combination of letters, numbers, and special characters. Avoid using personal information in your passwords.</p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Additional Security Features -->
            <div class="bg-gray-900/80 border border-indigo-500/20 rounded-xl overflow-hidden animate-fade-in shadow-lg">
                <div class="p-6 border-b border-indigo-500/10">
                    <h2 class="text-xl font-bold gradient-text">Advanced Protection</h2>
                    <p class="text-gray-400 mt-1">Additional security layers for your account</p>
                </div>

                <div class="p-6">
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <!-- Two-Factor Authentication -->
                        <div class="flex items-center justify-between p-5 rounded-xl bg-gray-800/50 border border-indigo-500/10 hover:bg-gray-800/80 transition-all duration-300">
                            <div class="flex items-center space-x-4">
                                <div class="w-10 h-10 rounded-xl bg-indigo-500/10 flex items-center justify-center">
                                    <i class="fas fa-mobile-alt text-indigo-500"></i>
                                </div>
                                <div>
                                    <h3 class="font-medium text-white">Two-Factor Authentication</h3>
                                    <p class="text-gray-400 text-sm">Add an extra layer of security</p>
                                </div>
                            </div>
                            <div class="relative">
                                <label class="inline-flex items-center cursor-pointer">
                                    <input type="checkbox" class="sr-only peer">
                                    <div class="w-11 h-6 bg-gray-700 rounded-full peer peer-checked:after:translate-x-full after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-indigo-600"></div>
                                </label>
                            </div>
                        </div>

                        <!-- Login Alerts -->
                        <div class="flex items-center justify-between p-5 rounded-xl bg-gray-800/50 border border-indigo-500/10 hover:bg-gray-800/80 transition-all duration-300">
                            <div class="flex items-center space-x-4">
                                <div class="w-10 h-10 rounded-xl bg-green-500/10 flex items-center justify-center">
                                    <i class="fas fa-bell text-green-500"></i>
                                </div>
                                <div>
                                    <h3 class="font-medium text-white">Login Alerts</h3>
                                    <p class="text-gray-400 text-sm">Get notified about new sign-ins</p>
                                </div>
                            </div>
                            <div class="relative">
                                <label class="inline-flex items-center cursor-pointer">
                                    <input type="checkbox" checked class="sr-only peer">
                                    <div class="w-11 h-6 bg-gray-700 rounded-full peer peer-checked:after:translate-x-full after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-green-600"></div>
                                </label>
                            </div>
                        </div>
                    </div>

                    <!-- Session Management -->
                    <div class="mt-6 p-5 rounded-xl bg-gray-800/50 border border-indigo-500/10 hover:bg-gray-800/80 transition-all duration-300">
                        <div class="flex items-center justify-between mb-4">
                            <div class="flex items-center space-x-4">
                                <div class="w-10 h-10 rounded-xl bg-purple-500/10 flex items-center justify-center">
                                    <i class="fas fa-laptop text-purple-500"></i>
                                </div>
                                <div>
                                    <h3 class="font-medium text-white">Active Sessions</h3>
                                    <p class="text-gray-400 text-sm">Manage devices where you're logged in</p>
                                </div>
                            </div>
                            <button class="px-4 py-2 rounded-lg bg-purple-500/10 text-purple-400 text-sm border border-purple-500/20 hover:bg-purple-500/20 transition-all duration-300">
                                <i class="fas fa-sign-out-alt mr-2"></i> Log Out All
                            </button>
                        </div>
                        
                        <div class="space-y-3">
                            <div class="flex items-center justify-between p-3 rounded-lg bg-gray-800/70 border border-gray-700">
                                <div class="flex items-center space-x-3">
                                    <i class="fas fa-desktop text-blue-400"></i>
                                    <div>
                                        <div class="text-sm text-gray-300">Current Browser</div>
                                        <div class="text-xs text-gray-500">Started <%= new Date().toLocaleDateString() %></div>
                                    </div>
                                </div>
                                <span class="px-2 py-1 rounded-md bg-green-500/10 text-green-400 text-xs">Active</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Initialize any interactive elements
            
            // Tooltip initialization (if needed)
            const tooltips = document.querySelectorAll('[data-tooltip]');
            tooltips.forEach(tooltip => {
                // Your tooltip code could go here
            });
            
            // Toggle switches
            const toggles = document.querySelectorAll('input[type="checkbox"]');
            toggles.forEach(toggle => {
                toggle.addEventListener('change', function() {
                    // Animation for toggle changes
                    const parent = this.closest('.rounded-xl');
                    parent.classList.add('shadow-glow');
                    setTimeout(() => {
                        parent.classList.remove('shadow-glow');
                    }, 500);
                });
            });
        });
    </script>
</body>
</html>