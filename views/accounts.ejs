<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Accounts - <%= settings.name %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet"/>
    <style>
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .animate-fade-in {
            animation: fadeIn 0.3s ease-out forwards;
        }
    </style>
</head>
<body class="bg-[#0F0F0F] text-white min-h-screen">
    <div class="flex">
        <!-- Sidebar -->
        <%- include('./components/navigation') %>
        
        <!-- Main Content -->
        <main class="flex-1 ml-72 p-8">
            <!-- Header -->
            <div class="flex items-center justify-between mb-8">
                <div>
                    <h1 class="text-3xl font-bold">Accounts</h1>
                    <p class="text-gray-400">Manage your connected accounts</p>
                </div>
            </div>

            <!-- Accounts Grid -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <!-- Discord Account -->
                <div class="bg-[#1A1A1A] rounded-xl p-6 hover:bg-[#242424] transition-all duration-300 animate-fade-in">
                    <div class="flex items-center justify-between mb-4">
                        <div class="flex items-center space-x-3">
                            <div class="w-12 h-12 rounded-xl bg-[#5865F2]/10 flex items-center justify-center">
                                <i class="fab fa-discord text-[#5865F2] text-xl"></i>
                            </div>
                            <div>
                                <h3 class="text-lg font-semibold">Discord</h3>
                                <p class="text-sm text-gray-400">Connected</p>
                            </div>
                        </div>
                        <div class="text-green-500">
                            <i class="fas fa-check-circle text-xl"></i>
                        </div>
                    </div>
                    <div class="flex items-center space-x-3 p-3 rounded-lg bg-[#242424]">
                        <img class="w-8 h-8 rounded-full" src="https://cdn.discordapp.com/avatars/<%= req.session.userinfo.id%>/<%= req.session.userinfo.avatar%>.jpg?size=1024" alt="Discord avatar">
                        <div>
                            <div class="text-sm font-medium"><%= req.session.userinfo.username%></div>
                            <div class="text-xs text-gray-400">#<%= req.session.userinfo.discriminator%></div>
                        </div>
                    </div>
                </div>

                <!-- Pterodactyl Account -->
                <div class="bg-[#1A1A1A] rounded-xl p-6 hover:bg-[#242424] transition-all duration-300 animate-fade-in">
                    <div class="flex items-center justify-between mb-4">
                        <div class="flex items-center space-x-3">
                            <div class="w-12 h-12 rounded-xl bg-blue-500/10 flex items-center justify-center">
                                <i class="fas fa-server text-blue-500 text-xl"></i>
                            </div>
                            <div>
                                <h3 class="text-lg font-semibold">Pterodactyl</h3>
                                <p class="text-sm text-gray-400">Connected</p>
                            </div>
                        </div>
                        <div class="text-green-500">
                            <i class="fas fa-check-circle text-xl"></i>
                        </div>
                    </div>
                    <div class="flex items-center space-x-3 p-3 rounded-lg bg-[#242424]">
                        <div class="w-8 h-8 rounded-full bg-blue-500/10 flex items-center justify-center">
                            <i class="fas fa-user text-blue-500"></i>
                        </div>
                        <div>
                            <div class="text-sm font-medium">Pterodactyl User</div>
                            <div class="text-xs text-gray-400">ID: <%= pterodactyl.id %></div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Security Section -->
            <div class="mt-8">
                <h2 class="text-2xl font-bold mb-6">Security</h2>
                <div class="bg-[#1A1A1A] rounded-xl p-6 hover:bg-[#242424] transition-all duration-300 animate-fade-in">
                    <div class="flex items-center justify-between">
                        <div class="flex items-center space-x-3">
                            <div class="w-12 h-12 rounded-xl bg-red-500/10 flex items-center justify-center">
                                <i class="fas fa-shield-alt text-red-500 text-xl"></i>
                            </div>
                            <div>
                                <h3 class="text-lg font-semibold">Two-Factor Authentication</h3>
                                <p class="text-sm text-gray-400">Add an extra layer of security to your account</p>
                            </div>
                        </div>
                        <button class="px-4 py-2 rounded-lg bg-red-500/10 text-red-400 hover:bg-red-500/20 transition-all duration-300">
                            Enable
                        </button>
                    </div>
                </div>
            </div>
        </main>
    </div>
</body>
</html>